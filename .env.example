# ログインパスワード（自宅・社内で共通）
VAULT_PASSWORD=3vbim8mibv

# セッション署名用（32文字以上のランダム文字列）
SESSION_SECRET=cApplication error: a server-side exception has occurred (see the server logs for more information).

Digest: 4056696919


# Vercel Blob（Vercel デプロイ時は自動設定される）
# 未設定の場合はローカルモード（packages/ に保存）
# 本番（Vercel）では BLOB_READ_WRITE_TOKEN の設定を推奨
# BLOB_READ_WRITE_TOKEN=
